

   
 <div class="col-lg-3 col-md-6 mb-5">
                <h4 class="font-weight-bold mb-4">Categories</h4>
                <div class="d-flex flex-wrap m-n1">
                  @foreach(Term tt in ViewBag.Term)
                      {
                      <a asp-action="Terms" asp-route-id="@tt.TermId" class="btn btn-sm btn-outline-secondary m-1">@tt.Name</a>
                     }

                </div>
            </div>